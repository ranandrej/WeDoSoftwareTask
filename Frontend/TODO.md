# TODO

- [x] Update `src/app/pages/home/home.ts` so `loading` reliably turns off after login API call using `finalize()`.

- [x] Ensure success/error messages are set/cleared in the correct branches (next/error).

- [x] Run tests/build (or `ng serve`) to verify UI state after success/failure.
